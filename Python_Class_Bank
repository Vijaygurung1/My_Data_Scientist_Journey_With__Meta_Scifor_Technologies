# Initialization: Sets the initial balance and ATM PIN.
# PIN Validation: Validate_pin checks if the provided PIN matches the stored PIN.
# Deposit Method: Validates the PIN and ensures the amount is positive before depositing.
# Withdraw Method: Validates the PIN, checks if the amount is positive and within balance, then withdraws.
# Check Balance is Validates the PIN before displaying the balance.

# Problem statement :- 

[#class name : Bank
#checks the atm pin for validity
#show verified msg
#able to withdraw money
#show existing balance

#similar for deposits from verification to showing balance after depositing
#able to deposit money
#checks the atm pin for validity ]


class Bank:
    def __init__(self, initial_balance=0, atm_pin="1234"):
        self._balance = initial_balance
        self._atm_pin = atm_pin

    def _validate_pin(self, pin):
        return pin == self._atm_pin

    def deposit(self, amount, pin):
        if self._validate_pin(pin) and amount > 0:
            self._balance += amount
            print(f"Deposited ₹{amount:.2f}. New balance: ₹{self._balance:.2f}")
        else:
            print("Invalid PIN or amount.")

    def withdraw(self, amount, pin):
        if self._validate_pin(pin) and 0 < amount <= self._balance:
            self._balance -= amount
            print(f"Withdrew ₹{amount:.2f}. New balance: ₹{self._balance:.2f}")
        else:
            print("Invalid PIN, amount, or insufficient balance.")

    def check_balance(self, pin):
        if self._validate_pin(pin):
            print(f"Current balance: ₹{self._balance:.2f}")
        else:
            print("Invalid PIN.")
            
account = Bank(initial_balance=1000, atm_pin="4321")
account.deposit(200, "4321")
account.withdraw(150, "4321")
account.check_balance("4321")
account.withdraw(2000, "4321")  # Insufficient balance
account.check_balance("1234")   # Invalid ATM PIN


# Below is the outcome -

[ Deposited ₹200.00. New balance: ₹1200.00
Withdrew ₹150.00. New balance: ₹1050.00
Current balance: ₹1050.00
Invalid PIN, amount, or insufficient balance.
Invalid PIN. ]
